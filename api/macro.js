const BLS='https://api.bls.gov/publicAPI/v2/timeseries/data/';
const FRED='https://fred.stlouisfed.org/graph/fredgraph.csv?id=';
async function blsSeries(ids){const now=new Date(),endYear=now.getUTCFullYear(),startYear=endYear-2,r=await fetch(BLS,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({seriesid:ids,startyear:String(startYear),endyear:String(endYear)})});if(!r.ok)throw Error('BLS HTTP '+r.status);const d=await r.json();if(d.status!=='REQUEST_SUCCEEDED')throw Error('BLS request failed');const out={};for(const s of(d.Results?.series||[]))out[s.seriesID]=(s.data||[]).filter(x=>x.period?.startsWith('M')).map(x=>({date:`${x.year}-${x.period.slice(1)}-01`,value:Number(x.value)})).sort((a,b)=>a.date.localeCompare(b.date));return out}
async function fred(id){const r=await fetch(FRED+encodeURIComponent(id),{cache:'no-store'});if(!r.ok)throw Error('FRED '+id+' HTTP '+r.status);const rows=(await r.text()).trim().split(/\r?\n/).slice(1).map(x=>{const [date,value]=x.split(',');return{date,value:Number(value)}}).filter(x=>x.date&&Number.isFinite(x.value));return rows}
const latest=a=>a?.length?a[a.length-1]:null,prev=a=>a?.length>1?a[a.length-2]:null;
function yoy(a){const x=latest(a);if(!x)return null;const y=a.find(v=>v.date===`${Number(x.date.slice(0,4))-1}-${x.date.slice(5)}`);return y?((x.value/y.value)-1)*100:null}
export default async function handler(req,res){try{const b=await blsSeries(['CUUR0000SA0','CUUR0000SA0L1E','LNS14000000','CES0000000001']);const[pce,corePce,dgs10,real10,dxy,dff]=await Promise.all(['PCEPI','PCEPILFE','DGS10','DFII10','DTWEXBGS','DFF'].map(fred));const cpi=b.CUUR0000SA0||[],core=b.CUUR0000SA0L1E||[],un=b.LNS14000000||[],nfp=b.CES0000000001||[];const d={cpi:{value:yoy(cpi)},coreCpi:{value:yoy(core)},pce:{value:yoy(pce)},corePce:{value:yoy(corePce)},unemployment:{value:latest(un)?.value},nfp:{value:latest(nfp)?.value,change:latest(nfp)&&prev(nfp)?latest(nfp).value-prev(nfp).value:null},tenYear:{value:latest(dgs10)?.value},realTenYear:{value:latest(real10)?.value},dxy:{value:latest(dxy)?.value},fedEffective:{value:latest(dff)?.value}};res.setHeader('Cache-Control','s-maxage=900, stale-while-revalidate=3600');return res.status(200).json({success:true,data:d,updatedAt:new Date().toISOString()})}catch(e){return res.status(502).json({success:false,error:e.message})}}
