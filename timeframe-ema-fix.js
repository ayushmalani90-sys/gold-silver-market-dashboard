(()=>{const addDailyButton=()=>{const wrap=[...document.querySelectorAll('.buttons')].find(x=>x.querySelector('[data-days="30"]'));if(!wrap||wrap.querySelector('[data-days="1"]'))return;const b=document.createElement('button');b.textContent='1D';b.dataset.days='1';wrap.insertBefore(b,wrap.firstElementChild);b.onclick=()=>{wrap.querySelectorAll('[data-days]').forEach(x=>x.classList.remove('active'));b.classList.add('active');const chartEl=document.getElementById('chart');if(typeof window.days!=='undefined')window.days=1;if(window.series&&window.visible){window.series.setData(window.visible());window.chart.timeScale().fitContent()}else if(chartEl)chartEl.dataset.lookback='1D'};const note=document.createElement('div');note.className='muted';note.style.marginTop='6px';note.textContent='1D/1M/3M/6M/1Y are daily-candle lookback periods. EMA 20/50/200 is calculated on the full daily history, so it correctly remains the same when only the lookback changes. A different EMA requires a different candle timeframe (e.g. 4H), which is not available from the current free historical feed.';wrap.parentElement.appendChild(note)};if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',addDailyButton);else setTimeout(addDailyButton,0)})();